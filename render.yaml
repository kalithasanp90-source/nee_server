services:
  - type: web
    name: nee-brain-v2
    env: python
    region: singapore
    plan: free

    buildCommand: "chmod +x build.sh && ./build.sh"

    startCommand: "uvicorn server:app --host 0.0.0.0 --port 10000"

    envVars:
      - key: PORT
        value: '10000'
